name: hep-analysis-workflow
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - pyarrow
  - pyyaml
  - uproot
  - awkward
  - hist
  - boost-histogram
  - matplotlib
  - scipy
  - tqdm
  - pip:
      - snakemake
  # Optional: enable once compatibility with your Python/Snakemake stack is checked.
  # - pyhf
